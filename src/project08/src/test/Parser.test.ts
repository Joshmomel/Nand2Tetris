import { test } from 'Parser'

/**
 * TODO: remove this test
 * Testing Jest only
 */
describe('here', () => {
  it('should +1', function () {
    const newNum = test(1)
    expect(newNum).toBe(2)
  })
})
